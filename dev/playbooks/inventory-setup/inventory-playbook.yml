---
- name: Compile inventory template locally
  hosts: localhost
  tasks:
    - name: compile inventory template
      template:
        src: "{{ playbook_dir }}/inventory.j2"
        dest: /cyberark/dev/inventory.tmp
